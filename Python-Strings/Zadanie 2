def CharFreq(str1):

    libr = {}

    for n in str1:

        keys = libr.keys()

        if n in keys:

            libr[n] += 1

        else:

            libr[n] = 1

    return libr

print(CharFreq('WhyGithubWhy'))

"""2. Write a Python program to count the number of characters (character frequency) in a string."""