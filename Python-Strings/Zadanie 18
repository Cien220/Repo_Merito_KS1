def AbsWrd(str):

    if len(str) > 3:

        return str[:3]
    
    else:

        return str

print(AbsWrd('lizak'))
print(AbsWrd('drzewo'))
print(AbsWrd('ferrari'))

"""18. Write a Python function to get a string made of the first three characters of a specified string. If the length of the string is less than 3, return the original string."""