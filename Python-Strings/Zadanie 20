def TrnRnd(str1):

    if len(str1) % 4 == 0:

        return ''.join(reversed(str1))
    
    return str1

print(TrnRnd('abcdefgh'))
print(TrnRnd('abudabi'))

"""20. Write a Python function to reverse a string if its length is a multiple of 4."""