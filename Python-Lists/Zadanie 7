a = [10, 20, 30, 20, 10, 50, 60, 40, 80, 50, 40]

UnDupe = set()

uniq = []

for x in a:

    if x not in UnDupe:

        uniq.append(x)

        UnDupe.add(x)

print(UnDupe) 

"""7. Write a Python program to remove duplicates from a list."""