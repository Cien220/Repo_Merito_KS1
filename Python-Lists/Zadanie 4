def MinList(lista):
    
    min = lista[0]

    for a in lista:

        if a < min:

            min = a

    return min

print(MinList([1, 3, 6, 0, -9, 7]))

"""4. Write a Python program to get the smallest number from a list."""