def MultList(items):

    total = 1

    for x in items:

        total *= x
    
    return total

print(MultList([1, 3, 6, 8, 11]))

"""2. Write a Python program to multiply all the items in a list."""