def MaxList(lista):
    
    max = lista[0]

    for a in lista:

        if a > max:

            max = a

    return max

print(MaxList([1, 3, 6, 0, -9, 7]))

"""3. Write a Python program to get the largest number from a list."""