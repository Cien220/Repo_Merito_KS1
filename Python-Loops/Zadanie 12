wiersz = []

while True:

    l = input()
    
    if l:

        wiersz.append(l.upper())

    else:

        break;

for l in wiersz:

    print(l) 
	
"""12. Write a Python program that accepts a sequence of lines (blank line to terminate) as input and prints the lines as output (all characters in lower case)."""