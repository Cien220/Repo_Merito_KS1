word = input("Podaj słowo do odwrucenia: ")

for char in range(len(word) - 1, -1, -1):

    print(word[char], end="")

print("\n")

"""5. Write a Python program that accepts a word from the user and reverses it."""