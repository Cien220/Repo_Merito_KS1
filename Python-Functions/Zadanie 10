def czyParzysta(l):

    enum = []
    
    for n in l:

        if n % 2 == 0:
            
            enum.append(n)
    
    return enum

print(czyParzysta([1, 2, 3, 4, 5, 6, 7, 8, 9])) 

"""Write a Python program to print the even numbers from a given list."""