def razy(liczby):

    total = 1
    
    for x in liczby:

        total *= x
    
    return total

print(razy((8, 2, 3, -1, 7))) 

"""Write a Python function to multiply all the numbers in a list."""