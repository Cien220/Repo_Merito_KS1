def unikalnaLista(l):

    x = []
    
    for a in l:

        if a not in x:

            x.append(a)
    
    return x

print(unikalnaLista([1, 2, 3, 3, 3, 3, 4, 5]))  

"""Write a Python function that takes a list and returns a new list with distinct elements from the first list."""