def get_numeric(prompt):
    while True:
        try:
            value = float(input(prompt))
            return value
        except ValueError:
            print("Error: Niewłaściwy input, wstaw liczbę.")

n1 = get_numeric("Wstaw pierwszą liczbę: ")
n2 = get_numeric("Wstaw drugą liczbę: ")
result = n1 * n2
print("Wynik obu liczb to:", result)

"""4. Write a Python program that prompts the user to input two numbers and raises a TypeError exception if the inputs are not numerical."""